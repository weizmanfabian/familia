# ============================================
# PERFIL DEV
# Backend corre LOCALMENTE en el IDE, DB en Docker.
# Conecta al puerto expuesto al host (DB_PORT_OUT).
# ============================================

spring.datasource.url=jdbc:postgresql://localhost:${DB_PORT_OUT}/${DB_NAME}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
