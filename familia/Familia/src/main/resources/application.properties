spring.application.name=${APP_NAME:Familia}
server.servlet.context-path=${APP_CONTEXT_PATH:/familia}
server.port=${APP_PORT_OUT:8089}

# Database Configuration
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:5439/${DB_NAME:familia}
spring.datasource.username=weizman
spring.datasource.password=YourStrong()Passw0rd
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.maximum-pool-size=5

# Logging Configuration
#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE